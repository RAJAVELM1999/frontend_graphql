
const state = {

  // name: "",
  // password: "",
  // nameRules: [
  //   (v) => !!v || "Name is required",
  //   (v) => (v && v.length <= 50) || "Name must be less than 50 characters",
  // ],

  // passrules: [
  //   (v) => !!v || "Password is required",
  //   // v => /.+@.+\..+/.test(v) || 'E-mail must be valid',
  // ],
  // nameVal: 'rajavel',
  // passVal: '1999',

  // username: "",
  // unRules: [
  //   (v) => !!v || "Username is required",
  //   (v) => (v && v.length <= 20) || "Name must be less than 20 characters",
  // ],
  // firstname: "",
  // fRules: [
  //   (v) => !!v || "Enter Your Firstname",
  //   (v) => (v && v.length <= 15) || "Firstname must be less than 15 characters",
  // ],
  // lastname: "",
  // lRules: [
  //   (v) => !!v || "Enter Your Lastname",
  //   (v) => (v && v.length <= 15) || "Lastname must be less than 15 characters",
  // ],
  // email: "",
  // eRules: [
  //   (v) => !!v || "Email is required",
  //   v => /.+@.+\..+/.test(v) || 'E-mail must be valid',
  // ],
  // password1: "",
  // pRules: [
  //   (v) => !!v || "Enter the Password ",
  //   (v) => (v && v.length >= 8) || 'Password at least 8 character',
  // ],
  // confirmpass: "",
  // cpRules: [
  //   (v) => !!v || "Confirm Password must be Same as password",

  // ],
  // rules: [
  //   (v) => !!v || "Upload Your Photo",
  //   v => !v || v.size < 7000000 || 'Image size should be less than 7 MB!',
  // ],
  // phno: "",
  // phnor: [
  //   (v) => !!v || "Phone Number is required",

  //   (v) => (v && v.length <= 10) || "Phone number must be 10 digits only",
  // ],

  // todoList: [
  //   { value: "testing purpose" },
  //   { value: "ithuvum work an nee sucessed than" }
  // ],



  // levelget: [

  // ],
  // skillsget: [],

  // subjectsget: [],

  // // levels:[],
  // course: [],

  // itemLevel: ['level1', 'level2', 'level3', 'level4'],
  // itemSubjects: ['subjects1', 'subjects2', 'subjects3'],

};

module.exports = { state };
